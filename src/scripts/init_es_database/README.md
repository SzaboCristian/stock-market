## The script initializes all Elasticsearch indices with the associated mappings. If indices were already previusly created and they need to be recreated from scratch, set recreate parameter to True.

### Indices:

- stocks
- stock_prices
- user_portofolios